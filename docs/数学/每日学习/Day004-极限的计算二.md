# Day 004 - 极限的计算（二）

> 学习日期：第4天  
> 所属章节：微积分 - 极限与连续  
> 难度：⭐⭐  
> 预计学习时间：2小时

## 🎯 学习目标

- [ ] 掌握两个重要极限
- [ ] 理解等价无穷小的概念
- [ ] 学会使用等价无穷小替换
- [ ] 了解无穷小的比较

## 📖 核心内容

### 1. 第一个重要极限 ⭐⭐⭐

$$\lim_{x \to 0} \frac{\sin x}{x} = 1$$

这是微积分中最重要的极限之一！

#### 几何证明思路

考虑单位圆，当 $x$ 很小时：
- 弧长 ≈ $x$
- $\sin x$ ≈ 弦长
- 当 $x \to 0$ 时，弧长与弦长趋于相等

#### 推广形式

$$\lim_{x \to 0} \frac{\sin(ax)}{x} = a$$

$$\lim_{x \to 0} \frac{\sin(ax)}{\sin(bx)} = \frac{a}{b}$$

$$\lim_{x \to 0} \frac{\tan x}{x} = 1$$

$$\lim_{x \to 0} \frac{x}{\sin x} = 1$$

#### 例1：直接应用

计算：$\lim_{x \to 0} \frac{\sin 3x}{x}$

**解答：**
$$\lim_{x \to 0} \frac{\sin 3x}{x} = \lim_{x \to 0} \frac{\sin 3x}{3x} \cdot 3 = 1 \cdot 3 = 3$$

#### 例2：配凑形式

计算：$\lim_{x \to 0} \frac{\sin 5x}{\sin 2x}$

**解答：**
$$\lim_{x \to 0} \frac{\sin 5x}{\sin 2x} = \lim_{x \to 0} \frac{\sin 5x}{5x} \cdot \frac{2x}{\sin 2x} \cdot \frac{5x}{2x}$$

$$= 1 \cdot 1 \cdot \frac{5}{2} = \frac{5}{2}$$

### 2. 第二个重要极限 ⭐⭐⭐

$$\lim_{x \to \infty} \left(1 + \frac{1}{x}\right)^x = e$$

或者等价形式：

$$\lim_{x \to 0} (1 + x)^{\frac{1}{x}} = e$$

其中 $e \approx 2.71828...$ 是自然对数的底。

#### 推广形式

$$\lim_{x \to \infty} \left(1 + \frac{a}{x}\right)^x = e^a$$

$$\lim_{x \to 0} (1 + ax)^{\frac{1}{x}} = e^a$$

$$\lim_{x \to \infty} \left(1 + \frac{1}{x}\right)^{bx} = e^b$$

#### 例3：直接应用

计算：$\lim_{n \to \infty} \left(1 + \frac{2}{n}\right)^n$

**解答：**
$$\lim_{n \to \infty} \left(1 + \frac{2}{n}\right)^n = \lim_{n \to \infty} \left[\left(1 + \frac{2}{n}\right)^{\frac{n}{2}}\right]^2 = e^2$$

#### 例4：配凑形式

计算：$\lim_{x \to \infty} \left(\frac{x+1}{x}\right)^x$

**解答：**
$$\lim_{x \to \infty} \left(\frac{x+1}{x}\right)^x = \lim_{x \to \infty} \left(1 + \frac{1}{x}\right)^x = e$$

### 3. 无穷小的概念

**定义：** 如果 $\lim_{x \to a} f(x) = 0$，则称 $f(x)$ 是当 $x \to a$ 时的**无穷小**。

**注意：** 
- 无穷小是一个**变量**，不是一个很小的数
- 0是唯一可以作为无穷小的常数

#### 无穷小的比较

设 $\alpha$ 和 $\beta$ 都是 $x \to a$ 时的无穷小，且 $\beta \neq 0$

1. **高阶无穷小：** 如果 $\lim \frac{\alpha}{\beta} = 0$，记作 $\alpha = o(\beta)$

2. **低阶无穷小：** 如果 $\lim \frac{\alpha}{\beta} = \infty$

3. **同阶无穷小：** 如果 $\lim \frac{\alpha}{\beta} = c \neq 0$

4. **等价无穷小：** 如果 $\lim \frac{\alpha}{\beta} = 1$，记作 $\alpha \sim \beta$

### 4. 常用等价无穷小 ⭐⭐⭐

当 $x \to 0$ 时：

$$\sin x \sim x$$
$$\tan x \sim x$$
$$\arcsin x \sim x$$
$$\arctan x \sim x$$
$$1 - \cos x \sim \frac{x^2}{2}$$
$$e^x - 1 \sim x$$
$$\ln(1 + x) \sim x$$
$$(1 + x)^{\alpha} - 1 \sim \alpha x$$
$$a^x - 1 \sim x \ln a$$

**这些公式非常重要，必须记住！**

### 5. 等价无穷小替换

**定理：** 在求极限时，乘除关系中的因子可以用等价无穷小替换。

**注意：** 加减关系不能直接替换！

#### 例5：等价替换

计算：$\lim_{x \to 0} \frac{\tan x - \sin x}{x^3}$

**解答：**

方法一：直接计算（较复杂）

方法二：等价替换
- $\tan x = \frac{\sin x}{\cos x}$
- $\tan x - \sin x = \frac{\sin x(1 - \cos x)}{\cos x}$
- $1 - \cos x \sim \frac{x^2}{2}$，$\sin x \sim x$

$$\lim_{x \to 0} \frac{\tan x - \sin x}{x^3} = \lim_{x \to 0} \frac{\sin x \cdot (1-\cos x)}{\cos x \cdot x^3}$$

$$= \lim_{x \to 0} \frac{x \cdot \frac{x^2}{2}}{1 \cdot x^3} = \frac{1}{2}$$

#### 例6：对数和指数

计算：$\lim_{x \to 0} \frac{e^{2x} - 1}{\ln(1 + 3x)}$

**解答：**

使用等价无穷小：
- $e^{2x} - 1 \sim 2x$
- $\ln(1 + 3x) \sim 3x$

$$\lim_{x \to 0} \frac{e^{2x} - 1}{\ln(1 + 3x)} = \lim_{x \to 0} \frac{2x}{3x} = \frac{2}{3}$$

#### 例7：幂函数

计算：$\lim_{x \to 0} \frac{(1+x)^3 - 1}{x}$

**解答：**

使用 $(1+x)^{\alpha} - 1 \sim \alpha x$：

$(1+x)^3 - 1 \sim 3x$

$$\lim_{x \to 0} \frac{(1+x)^3 - 1}{x} = \lim_{x \to 0} \frac{3x}{x} = 3$$

### 6. 金融应用：连续复利

#### 连续复利公式的推导

年利率 $r$，一年计息 $n$ 次，本金 $P$，则一年后：

$$A_n = P\left(1 + \frac{r}{n}\right)^n$$

当 $n \to \infty$（连续复利）：

令 $m = \frac{n}{r}$，则 $n = mr$，当 $n \to \infty$ 时 $m \to \infty$

$$A = \lim_{n \to \infty} P\left(1 + \frac{r}{n}\right)^n = \lim_{m \to \infty} P\left(1 + \frac{1}{m}\right)^{mr} = P \cdot e^r$$

**连续复利公式：** $A = Pe^{rt}$

#### 例8：实际应用

投资10000元，年利率8%，分别计算：
1. 年复利10年后的本利和
2. 连续复利10年后的本利和
3. 两者相差多少？

**解答：**

1. 年复利：$A = 10000 \times 1.08^{10} \approx 21589$ 元

2. 连续复利：$A = 10000 \times e^{0.8} \approx 22255$ 元

3. 相差：$22255 - 21589 = 666$ 元

**结论：** 连续复利略高于年复利，但差别不大。

## ✍️ 课后练习

### 基础题

1. 利用第一个重要极限计算：
   - (1) $\lim_{x \to 0} \frac{\sin 2x}{x}$
   - (2) $\lim_{x \to 0} \frac{\tan 3x}{x}$
   - (3) $\lim_{x \to 0} \frac{\sin 4x}{\sin 7x}$

2. 利用第二个重要极限计算：
   - (1) $\lim_{n \to \infty} \left(1 + \frac{3}{n}\right)^n$
   - (2) $\lim_{x \to \infty} \left(1 - \frac{1}{x}\right)^x$
   - (3) $\lim_{x \to 0} (1 + 2x)^{\frac{1}{x}}$

3. 利用等价无穷小计算：
   - (1) $\lim_{x \to 0} \frac{\sin 3x}{2x}$
   - (2) $\lim_{x \to 0} \frac{e^x - 1}{x}$
   - (3) $\lim_{x \to 0} \frac{\ln(1+2x)}{x}$

### 进阶题

4. 综合练习：
   - (1) $\lim_{x \to 0} \frac{1 - \cos x}{x^2}$
   - (2) $\lim_{x \to 0} \frac{\tan x - \sin x}{x^2 \sin x}$
   - (3) $\lim_{x \to 0} \frac{e^{3x} - 1}{\sin 2x}$

5. $1^{\infty}$ 型：
   - $\lim_{x \to \infty} \left(\frac{x+3}{x-1}\right)^x$

6. **金融应用题：**
   某基金的净值增长模型为：
   $$N(t) = N_0 \cdot \left(1 + \frac{0.12}{365}\right)^{365t}$$
   其中 $t$ 为年数。
   
   (1) 如果改用连续复利模型，公式是什么？  
   (2) 计算两种模型下10年后的差异（$N_0 = 1$）  
   (3) 哪种模型更合理？

### 挑战题

7. 证明：$\lim_{x \to 0} \frac{1 - \cos x}{x^2} = \frac{1}{2}$

8. 计算：$\lim_{x \to 0} \frac{\sqrt{1+x} - \sqrt{1-x}}{x}$（提示：用等价无穷小）

9. 计算：$\lim_{x \to 0} \left(\frac{e^x + e^{-x}}{2}\right)^{\frac{1}{x^2}}$

## 💡 重要提示

1. **两个重要极限必须熟记**
   - 第一个：$\lim_{x \to 0} \frac{\sin x}{x} = 1$
   - 第二个：$\lim_{x \to \infty} \left(1 + \frac{1}{x}\right)^x = e$

2. **等价无穷小的使用**
   - ✅ 乘除关系可以替换
   - ❌ 加减关系不能直接替换
   - 常用公式要背熟

3. **解题策略**
   - 看到三角函数 → 想重要极限
   - 看到 $(1 + \text{小量})^{\text{大量}}$ → 想第二重要极限
   - 复杂式子 → 考虑等价无穷小

4. **$e$ 的重要性**
   - 自然对数的底
   - 连续复利的基础
   - 出现在很多数学和金融公式中

## 🔗 相关知识

**前置知识：**
- [Day 003 - 极限的计算（一）](Day003-极限的计算一.md)

**后续学习：**
- [Day 005 - 极限的计算（三）](Day005-极限的计算三.md)
- 洛必达法则

## 📚 Day 003 练习题答案

1. (1) 6  (2) -1  (3) 12
2. (1) 1/4  (2) $2\sqrt{3}$  (3) 1
3. (1) 4/3  (2) 2  (3) 4
4. 32/3
5. (1) 100元，零利率时的价格  (2) -500（久期的负数）  (3) 债券价格对利率的敏感度

## 🎯 今日总结

今天我们学习了：
- ✅ 两个重要极限及其应用
- ✅ 无穷小的概念和比较
- ✅ 常用等价无穷小
- ✅ 等价无穷小替换法
- ✅ 连续复利公式的推导

**明天预告：** 极限的计算（三）- 洛必达法则

---

**这是极限计算的核心内容！** 🌟  
务必掌握两个重要极限和等价无穷小！

